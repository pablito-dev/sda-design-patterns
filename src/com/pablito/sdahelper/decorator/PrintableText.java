package com.pablito.sdahelper.decorator;

import java.io.FileNotFoundException;

public class PrintableText {
    //implement the Text interface
    //create Text field as a origin
    //create the right constructor
    //implement read() method read text from origin field in there replace all not printable characters using this regex
    //"[^\\p{Print}]"
    //return printable text from method
}
