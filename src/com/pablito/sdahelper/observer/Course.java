package com.pablito.sdahelper.observer;

public class Course {
    //Add attendee list
    //Extend Observable class
    //add addAttendee method which takes Attendee object this method should add new Attendee to list,
    //after this use setChanged() method to set changed status, then use notifyObservers with new attendee object to send it to the observers
}
